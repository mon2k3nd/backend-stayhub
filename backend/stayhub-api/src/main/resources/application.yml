spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stayhub_db
    username: postgres
    password: stayhub123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8089

# FIX: Secret phai co it nhat 32 ky tu (256 bits) cho HMAC-SHA256
jwt:
  secret: StayHub2026SuperSecretKeyForJWTAuthMustBe256BitsLong!@#Secure