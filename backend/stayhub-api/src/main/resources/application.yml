spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stayhub_db
    username: postgres
    password: stayhub123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
server:
  port: 8089