spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stayhub_db
    username: postgres
    password: stayhub123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

server:
  port: 8089

jwt:
  secret: StayHub2026SuperSecretKeyForJWTAuthMustBe256BitsLong!@#Secure
  expiration: 86400000

upload:
  directory: uploads/

logging:
  level:
    com.stayhub: INFO
    org.springframework.security: WARN